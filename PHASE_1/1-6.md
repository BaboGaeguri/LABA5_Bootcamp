# 1-6 스마트 경비 시스템 — 환경 세팅 기록

## 시스템 구성

```
핸드폰 핫스팟
    ├── 노트북 (Windows 11) — VSCode에서 작업
    └── 라즈베리파이 (laba@10.168.238.107) — 실행 환경
```

---

## 1. 핫스팟 연결

1. 핸드폰에서 핫스팟 활성화
2. 노트북 → 핫스팟 Wi-Fi 연결
3. 라즈베리파이 → 같은 핫스팟 Wi-Fi 연결
4. 라파 IP 확인 방법:
   - 핸드폰 핫스팟 앱 "연결된 기기" 목록 확인
   - 또는 라파 터미널에서: `hostname -I`
   - 또는 노트북 터미널에서: `arp -a`

> 현재 라파 IP: `10.168.238.107`

---

## 2. SSH 연결 (노트북 → 라파)

VSCode 터미널 또는 PowerShell에서:

```bash
ssh laba@10.168.238.107
```

- 접속 계정: `laba`
- 비밀번호 입력 후 연결
- 연결 성공 후 터미널에서 입력하는 모든 명령어는 **라파에서 실행**됨

---

## 3. 라파에 프로젝트 디렉토리 생성

SSH 연결된 상태에서 (라파 터미널):

```bash
mkdir -p ~/security_system/templates
```

생성되는 구조:
```
/home/laba/security_system/
├── main.py          ← 메인 Python 스크립트
└── templates/
    └── index.html   ← Flask 웹 UI (반드시 templates/ 하위에 위치)
```

> Flask의 `render_template("index.html")`은 자동으로 `templates/` 폴더를 탐색하므로 폴더명 고정

---

## 4. 파일 전송 (노트북 → 라파)

`scp` 명령어는 **SSH 연결 밖** (일반 터미널)에서 실행.

### 메인 스크립트 전송
```bash
scp "c:/Users/박상윤/Desktop/LABA5_Bootcamp/PHASE_1/1-6(revised).py" laba@10.168.238.107:~/security_system/main.py
```

### HTML 프론트엔드 전송
```bash
scp "c:/Users/박상윤/Desktop/LABA5_Bootcamp/PHASE_1/1-6(html_source).py" laba@10.168.238.107:~/security_system/templates/index.html
```

---

## 5. 라파에서 파일 확인

라파 터미널 또는 라파 모니터의 파일 매니저에서 확인:

```bash
find ~/security_system -type f
```

---

## 6. 트러블슈팅

### PermissionError: '/home/pi'
`main.py`의 `CAPTURE_DIR`이 `/home/pi`로 설정되어 있으나 라파 계정이 `laba`라서 권한 오류 발생.

**해결:** 라파 터미널에서 아래 명령어로 경로 수정:
```bash
sed -i 's|CAPTURE_DIR = "/home/pi"|CAPTURE_DIR = "/home/laba"|' ~/security_system/main.py
```

### scp 전송 실패 (No such file or directory)
라파에 폴더가 생성되기 전에 scp를 실행하면 실패함.
**반드시 SSH 터미널에서 `mkdir` 먼저 실행 후 scp 진행.**

---

## 7. 실행

SSH 연결된 터미널에서:

```bash
cd ~/security_system
python3 main.py
```

실행 후 노트북 브라우저에서:
```
http://10.168.238.107:5000
```
으로 접속하면 라이브 모니터링 화면 확인 가능.

### 실행 시 터미널 로그 예시
```
[HH:MM:SS] Camera started
[HH:MM:SS] Motion detected -> GUARD MODE
[HH:MM:SS] Authorized person detected
[HH:MM:SS] Intruder detected
[HH:MM:SS] INTRUDER CAPTURED -> /home/laba/intruder_YYYYMMDD_HHMMSS.jpg
[HH:MM:SS] No motion for 5 sec -> STANDBY MODE
```

---

## 사용 파일 정리

| 로컬 파일명 | 라파 경로 | 역할 |
|---|---|---|
| `1-6(revised).py` | `~/security_system/main.py` | Flask 서버 + 전체 보안 로직 |
| `1-6(html_source).py` | `~/security_system/templates/index.html` | 웹 모니터링 UI |
| `haarcascade_frontalface_default.xml` | (코드 내에서 자동 다운로드) | 얼굴 인식 모델 |

---

## 주요 설정값 (main.py)

| 항목 | 값 | 설명 |
|---|---|---|
| `MOTION_DIFF_THRESH` | 40 | 프레임 차이 임계값 |
| `MOTION_PIXEL_THRESHOLD` | 500 | 움직임 판정 최소 픽셀 수 |
| `NO_MOTION_TIMEOUT` | 5.0초 | 대기 모드 복귀 시간 |
| `SERVO_P_GAIN` | 0.12 | 서보 추적 P 게인 |
| `SERVO_DEADZONE` | 20px | 서보 미동작 허용 범위 |
| `INTRUDER_CAPTURE_COOLDOWN` | 5.0초 | 중복 촬영 방지 쿨다운 |
| `BLUE_RATIO_THRESHOLD` | 0.02 | 파란 카드 인식 최소 비율 |

---

## GPIO 핀 배치

| 기능 | GPIO 핀 (BCM) |
|---|---|
| 초록 LED (인가자) | GPIO 17 |
| 빨간 LED (침입자) | GPIO 27 |
| 서보모터 | GPIO 18 |
